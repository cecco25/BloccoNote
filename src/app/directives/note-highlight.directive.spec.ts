import { NoteHighlightDirective } from './note-highlight.directive';

describe('NoteHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
